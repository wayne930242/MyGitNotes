# 分頁與學習河道

Durable。使用者在本次對話確認以純 Markdown `---` 分頁，並回覆「好，實作？」授權實作已討論的方案。

| Question | Answer | Basis | Status |
|---|---|---|---|
| 正文格式 | 以頂層獨立 `---` 分頁，既有閱讀保持連續呈現 | 使用者明確要求 | confirmed |
| 本次可操作範圍 | 分頁閱讀、整份筆記正反面、隨選延後、依 status 階段安排間隔、到期篩選、手機操作 | 前述方案及實作授權 | confirmed |
| 未來功能 | 多題、反向卡、填空卡與線性簡報共用分頁及獨立卡片身分；本次預留資料結構 | 使用者要求評估未來 | confirmed |
| 學習資料 | 工作區側錄檔保存固定 ID、內容對應與完整事件，河道保存查詢及呈現 | 乾淨正文與跨裝置需求 | grounded |
| 內容修改 | 唯一且內容相同的頁面可重新定位；語義改變需重新配對並保留既有身分與歷史 | 穩定學習身分 | grounded |
| 倉庫 | 同一產品遠端的隔離 core checkout；完成後更新 trpg-notes main | 本機產品 checkout 有他人未提交修改 | grounded |

核心規則已確認：產品與使用者資料分支分離、共享領域邏輯位於 packages/core、既有來源授權與版本衝突檢查保留。Open consequential decisions: none.

## 河道流程修正（2026-09-14）

使用者明確指定閱讀／學習屬於河道版型，note status 是流轉階段。熟悉程度只決定抵達階段，間隔取該階段配置；輕鬆預設跳兩階，也可直達最後階段。這取代初版彈窗模式及排程與 status 分離的決策。一般隨選閱讀及 Markdown 純文字相容性繼續保留。

專屬泳道頁保留既有版型與操作。一般泳道預設維持卡片版型，明確切換才啟用單卡學習。配置集中在泳道編輯視窗；卡片翻頁及卡片切換各有獨立按鈕，翻閱不寫入事件。使用者已明確確認上述行為。

## 最終互動與共用按鈕修正

使用者改為學習獨立按鈕，直接進入全螢幕；view 保留縮圖、小、中。排序固定最早到期，學習篩選、閱讀切換、返回與設定共用單列 navbar，不顯示泳道標題或排序提示。此決策取代先前以閱讀／學習為 view 的呈現方式，status 流轉與 Markdown 分頁繼續保留。

使用者要求從共用按鈕根源處理 hover。查明既有兩份 CSS 重複定義與局部白字選中樣式造成衝突，因此建立共用 Button 及 ui-buttons.css，狀態透過 token 決定底色和文字，主色與 hover 前景依主題亮度計算。

## 精簡單卡操作

使用者授權依操作便利設計，主要翻頁、切卡與評分常駐底部，次要選卡、撤銷與延後收進更多操作。固定頁序取代學習／閱讀切換；滑動統一翻頁。篩選與階段同在學習配置，navbar 只留返回與設定。

## 固定操作位置

使用者指定熟悉程度在翻頁上方，重要按鈕位置固定，翻頁循環。撤銷常駐頂部設定旁；無操作時停用，底部配置保持一致。

使用者進一步允許熟悉程度先隱藏，再以展開動畫呈現。主要翻頁列保持原位，展開區向上增加；撤銷仍位於頁首。

## 共用進階設定與預設策略

使用者確認每個泳道保留學習入口；編輯泳道與學習頁共用預設收合的進階設定。四種熟悉程度獨立於階段數，預設沿用來源筆記本 YAML 順序；未設定時採系統一般狀態，預設排除 archived。既有手動策略保留。

## 上次移動時間（2026-09-14）

| Question | Answer | Basis | Status |
|---|---|---|---|
| 移動定義 | 四個熟悉程度按鈕成功保存，包含留在同階段 | 使用者本輪明確定義 | confirmed |
| 呈現時間 | 翻頁、略過、切卡維持純閱讀操作 | 使用者指定不用記錄呈現時間 | confirmed |
| 排程基準 | 上次移動時間加目的階段間隔，固定到期排序 | 已確認策略及現有核心邏輯 | grounded |
| 舊資料與撤銷 | 從有效階段評分事件補回時間，撤銷還原前次時間；沒有評分紀錄維持未設定 | 既有歷史與撤銷契約 | grounded |

本輪授權將移動時間納入本期實作。隨選維持手動選卡與到期排序，隨機混排留待另行定義。Open consequential decisions: none.
