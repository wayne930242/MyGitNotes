# Architecture image edit prompts

Mode: built-in imagegen, edit existing PNG images.

## English

Use case: precise-object-edit.
Asset type: English README architecture illustration.
Edit target: the supplied original GitHub Notes architecture PNG.
Edit the original image, preserving its composition, rounded cards, white background, gentle mint and lavender gradients, illustrated icons, arrow routing, generous spacing and polished presentation. Preserve the wide landscape aspect ratio and all existing content except the precise changes below.
1. Replace the large top title "GitHub Notes" with "MyGitNotes". Preserve the green book-and-Git logo and navy bold type.
2. In the REMOTE region, replace the existing GitHub-only provider card with a single shared provider-choice card featuring recognizable GitHub and orange GitLab icons, with clear labels "GitHub" and "GitLab" separated by "or". This means one selected platform per deployment. Keep this card between the Git sync arrows and the Vercel Serverless card. Fit the two platform options with balanced spacing and legible type; modestly widen this card within the same overall layout if necessary.
3. Add a concise, legible caption below the provider card within the remote panel: "GitLab.com or self-managed". Keep clear separation from the Redis card.
Keep all other existing exact text: "LOCAL", "REMOTE", "Browser", "Local API", "Markdown + Git", "Agent CLI / IDE", "Direct file access", "Sync", "Vercel Serverless", "Browser + MCP", "Redis", "YOU CONTROL", "Files · Repository · Deployment · Commits · Agent Access".
Preserve the original local workflow, two-way Git sync, browser/MCP to Vercel to selected provider connections, and Vercel-to-Redis connection. Render clean and crisp at high resolution with accurate typography.

## Traditional Chinese

Use case: precise-object-edit.
Asset type: Traditional Chinese README architecture illustration.
Edit target: the supplied original Traditional Chinese GitHub Notes architecture PNG.
Edit this original image, preserving the same composition, rounded cards, white background, subtle mint and lavender gradients, illustrated icons, arrow routing, generous spacing and polished presentation. Preserve its wide landscape aspect ratio and existing content except these precise changes.
1. Replace the top large "GitHub Notes" title with "MyGitNotes". Preserve the original green book-and-Git logo and navy bold title typography.
2. In the purple remote region, replace the GitHub-only provider card with one shared provider-choice card showing recognizable GitHub and orange GitLab icons, labeled "GitHub" and "GitLab" with the Traditional Chinese word "或" between the two options. It represents one chosen platform per deployment. Keep it between the Git sync arrows and Vercel Serverless. Make the logos and labels well spaced and legible; modestly widen the card within the existing layout if needed.
3. Add this legible caption directly below the provider card inside the remote panel: "GitLab.com 或自架 GitLab". Keep it clear of Redis.
Preserve all remaining text verbatim in Traditional Chinese: "本地", "遠端", "瀏覽器", "本地 API", "Markdown + Git", "本地 Agent CLI / IDE", "直接編輯檔案", "同步", "Vercel Serverless", "瀏覽器 + MCP", "Redis", "由你掌控", "檔案 · 儲存庫 · 部署 · 版本 · Agent 權限".
Preserve the local workflow, bidirectional Git sync, browser/MCP through Vercel to the selected provider, and Vercel-to-Redis connection. Keep all Chinese text Traditional Chinese with crisp accurate characters. Produce a clean high-resolution image in the original illustration style.
