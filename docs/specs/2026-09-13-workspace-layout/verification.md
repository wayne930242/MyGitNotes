# 介面驗證

## 實際介面

2026-09-13 使用瀏覽器檢查原版 localhost:5173 與產品預覽 localhost:5174。預覽讀取既有本機工作區；只執行導覽、篩選、主題與檢視操作，不寫入筆記、Agent 文件、工作區清單或資源。

- 原版側欄外層 16px 內距造成捲軸內縮；標籤內又有 max-height 與 overflow-y。四頁側欄及內容起點分別不同。
- 改版後桌面 1280px：四頁側欄右界均為 256px、標題左界均為 288px，標題頂端均約 91.3px。
- 側欄僅一個可捲動區，右界約 255.3px（其外為側欄邊框）。捲到底時最後標籤底端約 587px，固定頁尾頂端約 611px；主內容捲動位置保持 0。
- 320px、820px、1280px 下逐頁量測標題列、表單控制及 Agent 工具列，沒有控制項超出左右邊界。320px Agent 編輯區底端與提交列頂端一致；提交列不再覆蓋文件。
- 實際檢查淺色、暖棕及 GitHub 深色；資料夾入口可進入來源資料，設定側欄可跳到工作區清單，頁面標題保持固定。
- 手機資源選取後才顯示檢視、複製、刪除與移動列；操作列可換行。目錄與筆記本切換均沿用忙碌狀態鎖定。
- 三種筆記檢視切換與手機篩選抽屜開啟通過。

## 自動驗證

- 完整 pnpm build 通過；保留既有大型 bundle 提示，未新增建置錯誤。
- pnpm test：32 個測試檔、169 項測試通過（包含同期獨立提交的 Agent System 測試）。
- git diff --check 通過。

## 交付與適用性

產品以獨立 UI 提交交付，與 Agent System 功能提交分開。提交／推送與工作區更新結果另於交付時核對；此文件的介面檢查不代表已部署。

美感是否符合使用者預期仍待其檢視實際成品；不以測試通過代替這項判斷。
