# 驗證紀錄

2026-09-13，產品 `core`，隔離雙筆記本工作區 `/tmp/gn-notebook-navigation-m7fu33dl`。測試內容沒有寫入真實筆記工作區。

- 建立自訂泳道，將 Notebook A 與 B 的筆記釘在同一道；實際滑鼠拖曳把 B 移入另一泳道，來源內容不變。
- 泳道編輯視窗以鍵盤拖曳改變順序，重新命名後套用；修正原生 dialog 攔截所有鍵盤事件及 Select portal 被遮住的問題。
- 標籤 `focus` 動態列出 A、B 兩本筆記；資料夾 `notes/a/assets` 動態列出圖片。動態卡片沒有拖曳把手，也不註冊放置目標。
- 儲存後檢查真實 YAML 內容，再重新開啟 Screen，泳道順序、名稱、尺寸與跨筆記本參照仍存在。
- 從 Screen 的相對連結開啟另一筆記本的編輯器，URL 保留 `returnTo=screen` 與標題錨點。編輯器內圖片連結可預覽；點「在資源庫中顯示」後選中正確圖片。
- YouTube 釘選可產生 `youtube-nocookie.com/embed/...` iframe，含起始時間和播放參數。未以此宣稱外部影片的實際播放可用性；影片可能受到網路、地區或擁有者嵌入設定限制。
- 390px 螢幕：普通 Y 滾輪令泳道 scrollLeft 從 146 變成 0，頁面 scrollTop 保持 357.33；Alt + Y 後 scrollLeft 保持 0，頁面 scrollTop 變成 457.33。左右按鈕可捲動。
- 1280px、390px、320px 頁面無整頁水平溢出。窄螢幕的設定在右上角，筆記本圖示與選單同列；主導覽保留底部四個入口。
- Agent 原始碼輸入後立即切換 Screen，磁碟確認已保存最後一份輸入。先前已驗證切換筆記本的成功儲存及失敗時停留原本文件。
- HTTP 測試涵蓋本地 YAML、main 限制、符號連結、無效 YAML、過期版本與競爭寫入；遠端測試涵蓋未登入拒絕、版本衝突、只改一份 YAML、non-force 更新。
- 全套測試 37 個檔案、181 個案例；完整產品 build 通過。建置仍有既有大型 chunk 提示，Screen 已按需載入。

正式工作區匯入受 `trpg-notes` 另一批尚未提交的筆記改名／連結修改阻擋。產品版本可先推送並提供獨立預覽；不得自動暫存、提交或覆蓋那批使用者內容。
