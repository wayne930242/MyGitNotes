# Product paths the sparse Vercel deployment checks out (cone mode, one per line).
# Keep in sync with the `paths` filter in .github/workflows/deploy-vercel-sparse.yml;
# `pnpm check:vercel-sparse-paths --core` verifies both.
.github/vercel-sparse-paths.txt
.vercelignore
api
apps
docs/agent
package.json
packages
pnpm-lock.yaml
pnpm-workspace.yaml
scripts/lib
tsconfig.base.json
vercel.json
